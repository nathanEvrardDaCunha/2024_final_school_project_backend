import { Router } from 'express';import {	fetchAllUsersController,	registerController,	loginController,	banUserController,	promoteUserController, unbanUserController, deleteUserController, deleteSelfController} from './controllers';import {authenticateToken} from "../dashboard/middleware";const authRouter = Router();authRouter.post('/register', registerController);authRouter.post('/login', loginController);authRouter.put('/users/:userId/promote', authenticateToken, promoteUserController);authRouter.put('/users/:userId/ban', authenticateToken, banUserController);authRouter.put('/users/:userId/unban', authenticateToken, unbanUserController);authRouter.get('/users', authenticateToken, fetchAllUsersController);authRouter.delete('/users/:userId', authenticateToken, deleteUserController);authRouter.delete('/user/self', authenticateToken, deleteSelfController);export default authRouter;