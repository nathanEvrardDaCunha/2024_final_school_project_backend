import {PrismaClient, User, UserStatus} from '@prisma/client';const prisma = new PrismaClient();export async function findUserById(id: number): Promise<User | null> {	return prisma.user.findUnique({		where: { id },	});}export async function findUserByIdWithPassword(id: number): Promise<User | null> {	return prisma.user.findUnique({		where: { id },	});}export async function updateUserById(id: number, updateData: Partial<{	email: string;	firstname: string;	lastname: string;	birthDate: Date;	phoneNumber: string;	password: string;}>): Promise<User | null> {	return prisma.user.update({		where: { id },		data: updateData,	});}export async function updateUserStatus(userId: number, status: UserStatus): Promise<void> {	await prisma.user.update({		where: { id: userId },		data: { status },	});}