// src/controllers/serviceController.tsimport { Request, Response } from 'express';import { ServiceName } from '@prisma/client';import {bookService, createPaymentIntent, getServices} from "./services";export const createPaymentIntentController = async (req: Request, res: Response) => {	try {		const { serviceId, reservationId } = req.body;		const clientSecret = await createPaymentIntent(serviceId as ServiceName, reservationId);		res.json({ clientSecret });	} catch (error) {		console.error('Error in createPaymentIntentController:', error);		res.status(500).json({ error: 'Failed to create payment intent' });	}};export const bookServiceController = async (req: Request, res: Response) => {	try {		const { serviceName, reservationId } = req.body;		const bookedService = await bookService(serviceName as ServiceName, reservationId);		res.json(bookedService);	} catch (error) {		console.error('Error in bookServiceController:', error);		res.status(500).json({ error: 'Failed to book service' });	}};export const getServicesController = async (req: Request, res: Response) => {	try {		const services = await getServices();		res.json(services);	} catch (error) {		console.error('Error in getServicesController:', error);		res.status(500).json({ error: 'Failed to get services' });	}};