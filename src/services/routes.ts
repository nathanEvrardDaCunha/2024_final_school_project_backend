import express from 'express';import {authenticateToken} from "../dashboard/middleware";import {	bookServiceController,	createPaymentIntentController,	getServicesController,	getFreeServiceStatusController, getServicesWithAvisController, createAvis, deleteAvis} from "./controllers";const router = express.Router();router.post('/create-payment-intent', authenticateToken, createPaymentIntentController);router.post('/book-service', authenticateToken, bookServiceController);router.get('/', authenticateToken, getServicesController);router.get('/free-service-status', authenticateToken, getFreeServiceStatusController);router.get('/with-avis', authenticateToken, getServicesWithAvisController);router.post('/avis', authenticateToken, createAvis);router.delete('/avis/:serviceId', authenticateToken, deleteAvis);export default router;